# Known seed <-> dispatch-graph drift.
# Written by: python3 compass/scripts/seed-consistency-check.py --write-baseline
# This is DEBT, not permission. The check fails on anything NOT listed here.
# Fix an entry, then delete its line — the check tells you when one is resolved.
#
# The no-graph-steps entries are why open_workflow_run opens runs that create no task:
# the .md is prose and the seed has zero steps, so nothing is ever dispatched.
unseeded       advance
unseeded       create-epics
unseeded       dashboard
unseeded       metrics
unseeded       plan
unseeded       retro
unseeded       scan
unseeded       status

# `build` is TWO LAYERS and the counts differ on purpose. build.md is v1's dispatch graph — eight
# steps the orchestrator executes — and the seed has five, each a gated row. Seed ord 1-4 map onto
# build.md steps 1-4 and each invokes `--step N`, so the orchestrator executes ONE step per row
# rather than running its own copy of the graph. The seed is the procedural memory; build.md is v1's
# and goes when v1 does.
#
# NOT DEBT: there is no edit that resolves it while both exist, because build.md's steps 5-8
# (arbitrate, gate, mechanical checks, post-merge) are v1 concepts the seed deliberately does not
# carry. Recorded so the checker stays quiet. Do NOT trim build.md to match — that deletes the work
# the rows delegate to, and it was nearly done once already.
step-count     build

# `ops` is v1's, like the nine above it. Its seed row was dropped in the workflow restructure but
# `compass/workflows/ops.md` stays: v1's orchestrator still loads it, `_CODE_WORKFLOWS` still lists
# it, and deleting the file breaks a workflow that still runs. Unseeded is the right state — the app does
# not offer it, v1 still does — and that is exactly what the nine entries above record.
unseeded       ops


# The human review gates. `foundation-architecture`, `product-brief`, `tech-design` and `feature`
# each gained a review held by a PERSON where the .md dispatch graph has an `agent: reviewer` step,
# and product-brief gained rows besides. The seed is the procedural memory; these .md files are v1's
# dispatch graphs and go when v1 does, so they are not being rewritten to match.
#
# What changed and why: a document is not approved because its author says so. Each deliverable now
# carries two human gates — the author reviews their own work, then the next role reviews and
# approves it — and the machine criteria stay on the agent row while the judgment moved onto the
# hitl rows where a named role owns them.
step-shape     feature step 2
step-shape     foundation-architecture step 2
step-shape     foundation-architecture step 5
step-shape     foundation-architecture step 8
step-shape     tech-design step 2
step-count     product-brief

# The human review gates, second wave. `design-library`, `epics`, `sprint-plan`, `sprint`,
# `sprint-0` and `feature-architecture` gained the same shape the six above already carry: each
# authored deliverable is reviewed by its author and then reviewed and approved by an oversight
# role, so the seed's step counts run ahead of the .md dispatch graphs. Those graphs are v1's and
# are not being rewritten — the seed is what executes.
#
# `sprint-0` is the largest gap (15 -> 25) and the most consequential: `propose-staffing` carries
# `output: roster`, so approving it writes `member` rows, and until now no human gate stood in front
# of that at all.
step-count     design-library
step-count     epics
step-count     sprint
step-count     sprint-0
step-count     sprint-plan
step-shape     feature-architecture step 2

