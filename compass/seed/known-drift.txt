# Known seed <-> dispatch-graph drift.
# Written by: python3 compass/scripts/seed-consistency-check.py --write-baseline
# This is DEBT, not permission. The check fails on anything NOT listed here.
# Fix an entry, then delete its line — the check tells you when one is resolved.
#
# The no-graph-steps entries are why open_workflow_run opens runs that create no task:
# the .md is prose and the seed has zero steps, so nothing is ever dispatched.
unseeded       advance
unseeded       create-epics
unseeded       dashboard
unseeded       metrics
unseeded       plan
unseeded       retro
unseeded       scan
unseeded       status

# `build` is TWO LAYERS and the counts differ on purpose. build.md is v1's dispatch graph — eight
# steps the orchestrator executes — and the seed has two: hand off, then a human gate. Row 1 carries
# `output: code`, which spawns `compass.orchestrator.run build`, which runs all eight. Restating
# them as seed rows would dispatch each twice.
#
# NOT DEBT, unlike everything else in this file: there is no edit that resolves it, because the two
# numbers describe different layers. It is recorded so the checker stays quiet about a difference
# that is the design. Trimming build.md to match the seed deletes the work the seed delegates to —
# it was done once, in the session that wrote this, and caught before it shipped.
step-count     build

# `ops` is v1's, like the nine above it. Its seed row was dropped in the workflow restructure but
# `compass/workflows/ops.md` stays: v1's orchestrator still loads it, `_CODE_WORKFLOWS` still lists
# it, and deleting the file breaks a workflow that still runs. Unseeded is the right state — v2 does
# not offer it, v1 still does — and that is exactly what the nine entries above record.
unseeded       ops

