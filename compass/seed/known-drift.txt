# Known seed <-> dispatch-graph drift.
# Written by: python3 compass/scripts/seed-consistency-check.py --write-baseline
# This is DEBT, not permission. The check fails on anything NOT listed here.
# Fix an entry, then delete its line — the check tells you when one is resolved.
#
# The no-graph-steps entries are why open_workflow_run opens runs that create no task:
# the .md is prose and the seed has zero steps, so nothing is ever dispatched.

# `build` is TWO LAYERS and the counts differ on purpose. build.md is v1's dispatch graph — eight
# steps the orchestrator executes — and the seed has five, each a gated row. Seed ord 1-4 map onto
# build.md steps 1-4 and each invokes `--step N`, so the orchestrator executes ONE step per row
# rather than running its own copy of the graph. The seed is the procedural memory; build.md is v1's
# and goes when v1 does.
#
# NOT DEBT: there is no edit that resolves it while both exist, because build.md's steps 5-8
# (arbitrate, gate, mechanical checks, post-merge) are v1 concepts the seed deliberately does not
# carry. Recorded so the checker stays quiet. Do NOT trim build.md to match — that deletes the work
# the rows delegate to, and it was nearly done once already.
step-count     build

# `ops` is v1's, like the nine above it. Its seed row was dropped in the workflow restructure but
# `compass/workflows/ops.md` stays: v1's orchestrator still loads it, `_CODE_WORKFLOWS` still lists
# it, and deleting the file breaks a workflow that still runs. Unseeded is the right state — the app does
# not offer it, v1 still does — and that is exactly what the nine entries above record.


# The human review gates. Eleven entries stood here, covering `feature`, `feature-architecture`,
# `foundation-architecture`, `tech-design`, `product-brief`, `design-library`, `epics`,
# `sprint-plan` and `sprint` — .md dispatch graphs that disagreed with the seed about who reviews
# what. They are gone because the graphs were rewritten to match rather than baselined a second
# time: the seed dropped both the author's own review row and the independent reviewer step those
# graphs still carried, and each of those ten files now describes the rows that actually run.
#
# What the seed does now: one human gate per deliverable, held by a role that did not write it.
# `product-brief` has two, because its evidence is accepted before the brief is written on it.

# `sprint-0` is NOT that class, which is why it is still here. Its graph is 15 rows against the
# seed's 11, and the difference is not review steps: the graph carries a RACI, a ways-of-working, a
# tailored delivery plan and a kickoff that the seed has never had, files the SOW under a different
# role, and dispatches the timeline and the staffing plan as agent rows where the seed nests whole
# workflows. Trimming it is a decision about which of those rows are dropped and which are pending,
# not a transcription — so it stays debt until someone makes that call.
step-count     sprint-0

# The `unseeded` entries that used to sit here are gone with the class itself: a dispatch
# graph with no seed row is now NORMAL — the seed is a subset of the framework — and is
# reported under NOT TAKEN UP rather than failed on. A seed row with no graph still fails.
