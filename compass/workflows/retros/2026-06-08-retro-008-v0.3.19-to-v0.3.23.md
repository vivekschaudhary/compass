---
status: archive
altitude: framework
retro_number: 8
cycle: v0.3.19 → v0.3.23
improvements_covered: "#36 → #40"
date: 2026-06-08
period_start: 2026-06-08
period_end: 2026-06-08
period_days: 1
fires_after_improvement: 40
fired_at_improvement: 40
overdue_cycles: 0
created: 2026-06-08
author: framework-Architect (Compass-on-Compass)
parent_log: compass/workflows/improvements.md
consolidates_from: []
includes_artifact_analysis: false
---

# Retro #008 — framework — v0.3.19 → v0.3.23

**Reports patterns. Does not prescribe.** Per Principle #14 — periodic pattern review as structural defense against soft-spec rationalization. **Status: archive.** Convention candidates surfaced here feed future improvements via normal triggers; no auto-promotion.

## Meta — fourth consecutive on-time retro; single-day 5-improvement cycle

**Fired AT improvement #40** — hard line from Retro #007 held. **4th consecutive on-time retro** (Retro #005 → #006 → #007 → #008). `[hard-line-declaration]` empirically validated **past saturation** — 4 consecutive on-time fires after a 2-slip pre-codification baseline. Pattern is now structurally durable.

**Cadence: 5 improvements in 1 day (2026-06-08).** Comparable Retro #007 cycle: 5 improvements in 3 days; Retro #006: 5 improvements in 1 day (the prior single-day cycle). Single-day 5-improvement cycles are NOT one-off (now 2 instances in this catalog). Pattern: when a topical thread runs through a session (here: cap discipline → codification → workflow refactor → retro fire), 5 improvements compound naturally because each unlocks the next.

**Improvements covered:**

| # | Version | Date | Title |
|---|---|---|---|
| 36 | v0.3.19 | 2026-06-08 | `[user-as-load-bearing-oversight]` codified (14th Compass-original; 2nd observability-class member) |
| 37 | v0.3.20 | 2026-06-08 | Aspirational refinement of v0.3.19 (orchestrator catches mechanizable cases; user oversight shrinks to architectural-only residual) |
| 38 | v0.3.21 | 2026-06-08 | `pm.md` + `researcher.md` trimmed to fit OpenAI Custom GPT Instructions ~8000-char cap (3rd instance of `[agent-file-compression]`) |
| 39 | v0.3.22 | 2026-06-08 | `[agent-file-compression]` codified (15th Compass-original; 3rd observability-class member) + `compass/scripts/check-agent-cap.py` mechanical defense shipped — **first Compass-original codified alongside its mechanical defense in the same release** |
| 40 | v0.3.23 | 2026-06-08 | `/build` refactored to thin dispatch-graph shape (2nd workflow migrated; joins `/setup-product` v0.3.14); `engineer.respond-to-review` NEW task added; PM's `participates_in_workflows:` updated for `arbitrate-dispute` |

## What landed in this cycle

### 2 codifications (15 total Compass-originals)

1. **`[user-as-load-bearing-oversight]` v0.3.19** — codified the pattern descriptively: user is structurally part of the discipline loop, not external observer. 11+ instances at codification across v0.3.14 → v0.3.18. **2nd observability-class member** (joining `[role-boundary]` v0.3.4). Observability shape structurally validated.

2. **`[agent-file-compression]` v0.3.22** — codified the playbook + mechanical defense together. 3 instances at codification (delivery-manager v0.3.18; pm v0.3.21; researcher v0.3.21). **3rd observability-class member.** Observability becomes the largest non-enforcement class (3 vs enforcement's 4). **First Compass-original codified alongside its mechanical defense in the SAME release** — prior observability members shipped pattern first and script later (token-usage.py at v0.3.4+; check-freshness.py at v0.3.7 round 2, 4 releases late).

### 1 aspirational refinement (v0.3.20)

The v0.3.19 codification was descriptively correct (user IS load-bearing today) but didn't name the aspiration: SHRINK user-oversight surface to architectural-only over time. v0.3.20 added the aspiration + a taxonomy (orchestrator catches mechanizable cases · user retains architectural residual) + a forward-link candidate `[orchestrator-as-residual-shrinker]` (1 instance; codify when v0.4 orchestrator design enters scoping).

This was itself a user-load-bearing moment: user pushed back on v0.3.19 with *"ideally user as load bearing oversight is what we need to get away from..."* — sharpening the codification within hours of it landing.

### 1 artifact-pruning release (v0.3.21)

pm.md + researcher.md trimmed to fit ~8000-char cap. v0.3.18 closed only delivery-manager.md (3 releases earlier); v0.3.21 finished the job. 3rd instance of `[agent-file-compression]` — unlocked v0.3.22 codification.

### 1 workflow refactor (v0.3.23)

`/build` refactored to dispatch-graph shape. 2nd workflow migrated (joining `/setup-product` v0.3.14). NEW task `engineer.respond-to-review` added. PM's `participates_in_workflows:` extended for `arbitrate-dispute`. **Two codification candidates unlocked at 2 instances each:** `[workflow-as-dispatch-graph]` + `[task-ownership-locality]`.

## Drift signals — observed

### 1. **Same-release codification-plus-mechanism shipping** (v0.3.22 closed the lag)

Pre-v0.3.22, observability-class codifications shipped pattern first and mechanical defense later:

- `[role-boundary]` v0.3.4 → `token-usage.py` v0.3.4+ (later)
- `[freshness-check]` v0.3.3 → `check-freshness.py` v0.3.7 (4 releases late)

v0.3.22 broke the pattern: `[agent-file-compression]` + `check-agent-cap.py` shipped in the same release. **The pattern that emerged was itself an instance of v0.3.20's aspirational refinement working** (orchestrator/release-process catches the mechanizable case as it's named, not 4 releases later).

**Codification candidate (1 instance now):** `[codify-with-mechanical-defense-same-release]` — discipline that observability-class codifications ship their mechanical defense in the same release. Pattern-shape: when codifying a pattern with a known mechanical defense (script, CI gate, frontmatter convention), ship both together; don't defer the script to "next release" where it accumulates lag. 1 instance — wait for 2nd before codifying.

### 2. **Latent dependency surfacing during dispatch-graph refactor** (v0.3.23)

When `/build` refactored to dispatch-graph shape, PM's `participates_in_workflows:` had to be updated. PM's `arbitrate-dispute` task had been firing in `/build` disputes since the workflow's inception, but the frontmatter omitted `build` because the dispatch was implicit (inline workflow guidance, not a typed dispatch reference).

**Codification candidate (1 instance now):** `[explicit-dispatch-surfaces-latent-participation]` — when a workflow refactors to dispatch-graph shape, frontmatter on dispatched agents needs to follow. Future refactors should check: does this dispatch surface a previously-implicit agent that now needs `participates_in_workflows:` updated? Pattern-shape: dispatch-graph refactors are LATENT-DEPENDENCY-SURFACING events. 1 instance — wait for 2nd (likely `/fix` refactor) before codifying.

### 3. **Cap-compounding closed structurally** (v0.3.22)

Pre-v0.3.22, agent-file cap-check was manual `wc -c` that lapsed for 3 releases (v0.3.15 flagged → v0.3.16, v0.3.17, v0.3.18 elapsed without fix) while three files compounded to 158% / 151% / 273% of cap. v0.3.22 shipped `check-agent-cap.py` — structural defense in place. **Drift signal Retro #007 named is now mechanically closed.**

**Worth verifying:** does engineer.md (currently 13,108 chars, WARN — non-chatgpt) actually NEVER target chatgpt long-term? Or is it a 3rd-instance compression candidate-in-waiting? If the answer is "engineer doesn't need chatgpt support," the WARN is fine forever. If a future migration adds chatgpt to engineer.md's preferred_hosts, the WARN auto-becomes FAIL on the next `check-agent-cap.py` run — no prose update needed.

### 4. **Cadence pattern: single-day 5-improvement cycles are now 2 instances** (Retro #006 + this one)

Retro #006 (v0.3.9 → v0.3.13, 2026-06-02) was the first single-day 5-improvement cycle. This Retro #008 (v0.3.19 → v0.3.23, 2026-06-08) is the second. Pattern: when a topical thread runs through a session, 5 improvements compound naturally (each unlocks the next).

**NOT a drift signal — observation only.** Cycle cadence varies (3 days in Retro #007, 1 day in #006 and #008); the cadence shape isn't the signal. The signal worth watching: **does fast cadence reduce per-release verification rigor?** Compare: v0.3.22 ENABLED fast cadence (check-agent-cap.py automates what was manual); v0.3.23 BENEFITED from fast cadence (workflow refactor in 60 min because agents already migrated). The pattern is positive feedback loop, not skipped verification.

## Convention candidates surfaced

### 4 candidates at 2+ instances (codification-ready)

1. **`[workflow-as-dispatch-graph]`** — 2 instances (`/setup-product` v0.3.14 + `/build` v0.3.23). Workflows become thin dispatch graphs naming `<agent>.<task>` references; methodology lives in agents. **PROMOTION CANDIDATE.** Architectural-discipline class would grow from 2 → 3 members. 3rd-instance candidate `/fix` (Engineer's `fix-bug` task already declared); could codify NOW per 2-instance evidence + clean structural shape, OR wait for `/fix` refactor.

2. **`[task-ownership-locality]`** — 2 instances (`/setup-product` = 4 tasks across pm + researcher + delivery-manager; `/build` = 5 tasks across engineer + reviewer + pm). Tasks belong to agents, not workflows; dispatch graph names them but doesn't OWN them. **PROMOTION CANDIDATE.** Could be same-class as `[workflow-as-dispatch-graph]` (architecture-discipline) or its own class. 3rd-instance candidate: any subsequent workflow refactor.

3. **`[agent-file-compression]`** — already codified v0.3.22. Listed here for cycle traceability only.

4. **`[user-as-load-bearing-oversight]`** — already codified v0.3.19. Listed here for cycle traceability only.

### 3 candidates at 1 instance (wait for 2nd)

1. **`[codify-with-mechanical-defense-same-release]`** — 1 instance (v0.3.22 ships `[agent-file-compression]` + `check-agent-cap.py` together). Discipline that observability-class codifications ship their mechanical defense in the same release, not 4 releases later. Wait for 2nd instance before codifying.

2. **`[explicit-dispatch-surfaces-latent-participation]`** — 1 instance (v0.3.23 surfaces PM's missing `build` in `participates_in_workflows:`). Pattern-shape: dispatch-graph refactors are LATENT-DEPENDENCY-SURFACING events. Wait for 2nd instance (likely `/fix` refactor).

3. **`[external-primary-with-cached-pointer]`** — 1 instance from user's consumer-project work 2026-06-08 (Wealth-at-Fingertips: GDrive canonical + repo cache with `primary:`, `source:`, `last_synced:` frontmatter + dual-flip approval). Persisted to memory file `queued_codification_candidates_2026-06-08.md`. Wait for 2nd instance.

4. **`[host-preference-validation]`** — 1 instance from user's evidence on consumer project (pm.md + researcher.md `preferred_hosts: [chatgpt, claude, codex, gemini]` ordering is empirically wrong; should be Claude-first per Claude-desktop validation + ChatGPT quality miss). Persisted to memory. Wait for 2nd instance.

### Persistent forward-link candidates from prior retros

- **`[framework-repo-guard]`** (Retro #007) — still 1 instance (freedom-bootstrap mistake). No new instance this cycle. Hold.
- **`[orchestrator-as-residual-shrinker]`** (v0.3.20) — still 1 instance (the declaration itself). Codify when v0.4 orchestrator design enters scoping. Hold.

## PROMOTION recommendations for canon (this retro fires)

**RECOMMENDATION 1: `[workflow-as-dispatch-graph]` — PROMOTE TO CANON at 2 instances.**

Justification: structural shape is clean and stable (mirror /setup-product → /build with zero gate/postcondition variance); both instances PRESERVED behavior (verified by content-sanity in both refactors); 3rd-instance candidate `/fix` is so similar that codifying at 2 instances and applying to `/fix` would be MORE disciplined than waiting (the codification establishes the playbook /fix follows). Would be **3rd architecture-discipline class member** (joining `[agent-as-surface-independent-unit]` v0.3.14 + `[fractal-retro]` v0.3.17). Architecture-discipline shape gets 3rd member → durably validated.

**RECOMMENDATION 2: `[task-ownership-locality]` — DEFER to 3rd instance.**

Justification: structurally inseparable from `[workflow-as-dispatch-graph]` (every dispatch-graph instance demonstrates both). Codifying both in same release is redundant — they're co-extensive. Wait for `/fix` refactor (3rd instance of each); judge then whether they're one pattern or two. Possible naming consolidation: `[workflow-as-dispatch-graph-of-agent-tasks]` covers both shapes in one canon entry.

**RECOMMENDATION 3: `[codify-with-mechanical-defense-same-release]` — DEFER to 2nd instance, but DECLARE explicitly in next release notes.**

Justification: 1 instance is genuinely intriguing; the discipline is non-obvious (prior observability codifications didn't follow it) and the v0.3.22 shape is too clean to be coincidence. But codifying at 1 instance violates `[declare-not-implement]`. Compromise: declare explicitly in v0.3.24+ release notes so when v0.3.x codifies its next observability-class pattern, the question "ship the mechanical defense in the same release?" is in front of the framework's eyes.

## Watch-for (next cycle, v0.3.24 → v0.3.28)

1. **`/fix` refactor** — natural 3rd instance of `[workflow-as-dispatch-graph]`. If PROMOTE Recommendation #1 lands, `/fix` becomes the codified-shape test case. Otherwise `/fix` is the 3rd-instance trigger for codification.

2. **Reviewer-on-Codex empirical validation** — Reviewer's `preferred_hosts: [codex, gemini]` is structurally enforced now (v0.3.16). Worth confirming the cross-host-integrity claim holds with N+ real reviews where Engineer was Claude and Reviewer was Codex. v0.3.16 codification cited 1 empirical instance (CB-1.4); 2nd instance through repeated use this cycle would harden the claim.

3. **engineer.md cap WARN state** — does it stay WARN forever (Engineer never targets chatgpt) or eventually become FAIL trigger (chatgpt support added)? Track. If WARN stays for 5+ releases without intent to add chatgpt, consider relaxing the WARN-vs-FAIL distinction for non-chatgpt-target agents (currently WARN is INTEL for future migration; if no migration coming, the WARN noise has no signal).

4. **2 codification candidates from consumer work** (`[external-primary-with-cached-pointer]` + `[host-preference-validation]`) — surface as 2nd-instance triggers fire. User explicitly said *"lets note this and we will come back."* These are watch-for, not deferred-action.

5. **Forward-link candidates from this retro** — `[codify-with-mechanical-defense-same-release]` + `[explicit-dispatch-surfaces-latent-participation]`. Both at 1 instance; watch for 2nd in future cycles.

6. **Cadence sustainability** — 5 improvements in 1 day is great IF verification rigor holds. v0.3.22 + v0.3.23 both shipped with `check-agent-cap.py` verification + content-sanity checks; no behavior-change regressions caught. Continue rigor-with-cadence; flag if a fast-cadence release ships a regression.

7. **`[hard-line-declaration]` cadence-enforcement durability** — 4 consecutive on-time retros now. Pattern is structurally durable past saturation. Consider whether the verbose counter-visibility prose in `compass/workflows/improvements.md` header can be tightened (the discipline is now muscle memory; the prose may be over-correction). Don't relax YET; wait one more cycle (Retro #009) to confirm.

## Promotion decisions (final)

Per the recommendation analysis above:

**This retro PROMOTES `[workflow-as-dispatch-graph]` TO CANON.** Codify in v0.3.24 (next release) per the canonical PROMOTE → CODIFY path. **3rd architecture-discipline class member.** Catalog grows 7 shapes / 15 patterns → 7 shapes / 16 patterns; architecture-discipline (2 → 3).

**This retro DEFERS `[task-ownership-locality]` to 3rd-instance evaluation** at `/fix` refactor. Consider naming consolidation with `[workflow-as-dispatch-graph]` at that time.

**This retro DECLARES `[codify-with-mechanical-defense-same-release]` as an explicit watch-for** — surface in v0.3.24 release notes so the next observability-class codification opportunity sees the question.

## Anti-signals — what we did NOT see

Worth noting for honest cycle accounting:

- **No on-time-retro regression.** 4 consecutive on-time. Pattern stable.
- **No phantom-write / wrong-volume issues this cycle.** The volume-location memory file is doing its job.
- **No framework-vs-project boundary violations this cycle.** Memory file holding. `[framework-repo-guard]` candidate still at 1 instance.
- **No paraphrasing UX Writer copy in builds this cycle.** Discipline holding (no /build runs this cycle to test; flag if a future cycle has many builds and the discipline lapses).
- **No silent skips.** Every skip logged as DRI Decision (per Compass discipline).

## Honest meta-observation

This retro covers 5 improvements all shipped within a single user-session (2026-06-08 morning → afternoon). The framework's pattern-recognition capability is itself accelerating: v0.3.22 codified a pattern by observing 3 instances; the codification + script + AGENTS.md update + README + CHANGELOG + improvements entry all shipped in one ~45-min release. v0.3.23 refactored a workflow in 60 min because the agents it dispatches were already migrated. The framework is becoming MORE mechanical over time, which is the v0.3.20 aspirational refinement working in real-time.

The 100% user-driven origin pattern from Retro #007 continues here:
- v0.3.19 codification triggered by user's "Not worried.. what else is in the improvements" prompt after the freedom-bootstrap recovery
- v0.3.20 refinement triggered by user's "ideally user as load bearing oversight is what we need to get away from..."
- v0.3.21 trim triggered by user's "lets start task 1"
- v0.3.22 codification + script triggered by user's "lets roll to task 2"
- v0.3.23 refactor triggered by user's "task 3 please"

Every single improvement this cycle had user-initiated dispatch. **`[user-as-load-bearing-oversight]` continues to validate at every cycle boundary.** The framework cannot self-prioritize improvements without user signal; that's the structural truth v0.3.19 codified.

## Closes

Retro #008 closes the v0.3.19 → v0.3.23 cycle. Counter resets to #45 horizon (Retro #009 fires after improvement #45). Hard line continues. Promotion of `[workflow-as-dispatch-graph]` triggers next release as v0.3.24.

---

_Retro #008 fired 2026-06-08 ON TIME at improvement #40 per the v0.3.17 `[fractal-retro]` schema (`altitude: framework`, `consolidates_from: []` — no child retros consolidated since lower-altitude retros not yet adopted in framework repo). 4th consecutive on-time retro. Hard line from Retro #007 held._
