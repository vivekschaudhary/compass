---
status: archive
altitude: framework
retro_number: 10
cycle: v0.4.0-alpha-2 → field learnings (#49 + #50)
improvements_covered: "#46 → #50"
date: 2026-06-09
period_start: 2026-06-08
period_end: 2026-06-09
period_days: 2
fires_after_improvement: 50
fired_at_improvement: 50
overdue_cycles: 0
created: 2026-06-09
author: framework-Architect (Compass-on-Compass)
parent_log: compass/workflows/improvements.md
consolidates_from: []
includes_artifact_analysis: false
---

# Retro #010 — framework — v0.4.0-alpha-2 → field learnings

**Reports patterns. Does not prescribe.** Per Principle #14 — periodic pattern review as structural defense against soft-spec rationalization. **Status: archive.** Convention candidates surfaced here feed future improvements via normal triggers; no auto-promotion.

## Meta — 6th consecutive on-time retro; first retro with consumer-project evidence

**Fired AT improvement #50** — hard line held. **6th consecutive on-time retro** (Retro #005 → #010). `[discipline-as-muscle-memory]` watch-for from Retro #009 is now confirmed: the retro fired on time with the tightened 1-sentence header prose. The discipline held without explicit 3-sentence counter-visibility pressure. Pattern is structural habit, not scaffolding-dependent.

**First retro with consumer-project evidence.** Improvements #49 and #50 originated from a user running Compass on a real project (crypto app) — not framework dogfooding, not theoretical. This is the first time the retro cycle contains user-as-consumer signal. The framework's "start sending" goal is no longer hypothetical: a real project was run, friction was felt, and runtime failures occurred.

**Improvements covered:**

| # | Version | Date | Title |
|---|---|---|---|
| 46 | v0.3.27 | 2026-06-08 | `/create-brief` → dispatch-graph (4th workflow); pm.md `draft-brief` task made real |
| 47 | v0.3.28 | 2026-06-08 | Designer + UX Writer agents migrated — completes Product pack |
| 48 | v0.4.0-alpha-2 | 2026-06-08 | Orchestrator multi-host dispatch — Reviewer → Codex/OpenAI, not Claude |
| 49 | (logged) | 2026-06-09 | "Minimize friction" as explicit Compass principle — field signal, crypto app |
| 50 | (logged) | 2026-06-09 | Engineer prod-parity discipline — runtime failures despite passing e2e tests |

## Pattern analysis

### PROMOTE to canon

**`[discipline-as-muscle-memory]`** — **PROMOTE RECOMMENDED.** Retro #009 watch-for stated: "if the retro cadence holds for 2 more cycles WITHOUT explicit counter-visibility pressure, then the pattern is real." Retros #009 and #010 both fired on time with tightened header prose (1 sentence, no named consequence). 6 consecutive on-time retros. The discipline is structural — it no longer requires explicit reminder scaffolding. **Pattern:** once a cadence commitment has fired 5+ times consecutively, the counter-visibility apparatus can be reduced to a single line; the habit is self-sustaining. Codification target: add to canon.md under discipline-class (or observability-class — adjacent to `[user-as-load-bearing-oversight]`).

### DEFER

**`[goal-driven-high-cadence-arc]`** — still at 1 instance. Retro #010 does not add a new instance (this cycle spanned 2 days, not same-day back-to-back). Hold.

**`[orchestrator-as-residual-shrinker]`** — still 0 real instances. The orchestrator hasn't yet caught a discipline violation in the wild; it's plumbing-class still. Hold until v0.4-beta ships with full workflow run.

**`[host-preference-validation]`** (queued memory) — still 1 instance. No new evidence this cycle. Hold.

### New watch-for candidates (this cycle)

**`[consumer-as-signal-source]`** (NEW watch-for) — improvements #49 and #50 are the first improvements where the origin is not framework dogfooding but consumer usage evidence. The crypto app run produced two actionable signals in one session (friction + prod-parity). **Pattern hypothesis:** consumer project runs are higher-quality signal sources than framework self-improvement sessions because they surface failure modes that only appear under real usage conditions (prod env divergence, workflow friction in context). **1 instance.** Trigger: if a second consumer project run produces improvement candidates in a single session, codify.

**`[logged-not-yet-implemented]`** (NEW watch-for) — improvements #49 and #50 are logged but not implemented. This is the first time improvements have been logged without same-session implementation. The framework has always implemented improvements immediately. **Pattern hypothesis:** some improvements benefit from deliberate deferral (log signal, implement when the surrounding context matures). Risk: logged improvements accumulate and never ship (the same drift that caused Retro #004 to fire 2 cycles late). **Trigger:** if a logged-but-not-implemented improvement survives 2 retro cycles without implementation, treat it as a drift signal.

### Drift signals

**#49 and #50 are unimplemented.** Filed as signals; no version bump; no code change. Per `[logged-not-yet-implemented]` watch-for above — if these are still unimplemented at Retro #011, that is a drift signal. The right time to implement: when a natural session arc creates the right context (e.g., Engineer agent work session → implement #50 prod-parity then).

**Friction (#49) lacks a metric.** "Minimize friction" is a principle without a measurement mechanism. Without a metric, it becomes wallpaper. Before implementing: define what friction means in measurable terms (number of human decisions per workflow run? time-to-first-artifact? prompts-before-HITL?). Implementation deferred until metric defined.

**7 agents still in `compass/roles/`** (down from 9 after v0.3.28 Designer + UX Writer migration). Remaining: Support · Enterprise Architect · Security Reviewer · Tech Writer · Scanner · Triage (new) · Automation (new) · Launch Engineer (new). MVP pack per `compass/framework/mvp.md` needs Automation + Launch Engineer + Triage + Observability. None shipped yet.

## What "start sending" needs from here

Per `compass/framework/mvp.md` "start sending" checklist:

- [x] `compass run setup-product` — walkable; alpha-2 writes artifacts; multi-host routing working
- [x] `compass run build` — walkable; Reviewer routes to Codex (cross-model independence restored)
- [ ] All Product pack + Build pack agents in `compass/agents/` — Product pack complete (v0.3.28); Build pack missing Automation (new)
- [ ] HITL context passing — alpha-2 pauses + prompts; HITL approval text not yet fed to next step
- [ ] New user can clone + set API keys + `compass run setup-product` + get artifact — structurally ready; needs real consumer test

**New gap surfaced this cycle:** friction and prod-parity are now logged failure modes from a real consumer run. "Start sending" requires these to be addressed before broad distribution — a user who hits prod failures and workflow friction on first run will not continue.

## Counter reset

**Counter resets to #55 horizon.** Next retro (Retro #011) fires after improvement #55.
